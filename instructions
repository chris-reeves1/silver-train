TASK 1 -app 

simple version to start:

    repo - app.py, nginx.conf, reqs.txt, Dockerfile(s), Jenkinsfile

    local setup on ec2: add jenkins to the docker group + reboot.

    Set up a pipeline job on jenkins with a webhook.

    Jenkinsfile stages:
        - clean-up (delete container)
        - set-up (network)
        - build images
        - run containers

    manual check - curl or check in browser. 

Stretch 1:
    Trivy:
        1) File system scan first (run after clean-up/set-up to catch issues from the repo) + archive
        2) After image is built run an image scan + archive. 
    
    Unit test
        1) Run a unit test (will require some set up on the ec2...)
        2) Use catchError() in the stage and mark failing tests as 'unstable'

                import requests
                import unittest
                
                class FlaskAppTests(unittest.TestCase):
                    BASE_URL = "http://localhost:80"
                
                    def test_home_page(self):
                        response = requests.get(f"{self.BASE_URL}/")
                        self.assertEqual(response.status_code, 200)
                        self.assertIn("Hello", response.text)
                        self.assertIn("I'm currently running in", response.text)


                if __name__ == "__main__":
                    unittest.main()


Stretch 2 - switch to professional version of app and add:
    - Gate the quality/security results - must manually approve before continuing (use input)
    - Push image(s) to Dockerhub - 1) add token form dockerhub to Jenkins credentials.
                                   2) Add credential to environment{} section in Jenkinsfile.
                                   3) Use environment vars for things like tags and image names.
    - Create and archive build and image metadata files including SBOM. 


Stretch 3:
    1) Run a check for image size and block images over 200mb.
    2) Run initial scans/tests in parallel (so they fail fast)
    3) Image builds can be done in parallel too!
    4) Add smoke tests after containers run.

Stretch 4:
    1) Paramatrize the build with an optional slim build for the image - https://github.com/slimtoolkit/slim

Stretch 5:
    Request an upgrade to ec2 and run Sonarqube and perfrom static code analysis tests. Dont try it on current t3.mediums.  









